package com.example.gamerps.gameplay.engine;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

public class GamePlayRpsEngineTest {

    private GamePlayRpsEngine gamePlayRpsEngine;

    @BeforeEach
    public void setUp() {
        //gamePlayRpsEngine = new GamePlayRpsEngine();
    }

    @Test
    public void ensure_RPC_PlayRound_Generate_a_Valid_GameRound() {
        //final GameRoundResult gameRoundResult = gamePlayRpsEngine.playRound();
        //Assertions.assertThat(gameRoundResult).isNotNull();
        //Assertions.assertThat(gameRoundResult.getPlayerRoundResult().size()).isEqualTo(2);
    }
}
